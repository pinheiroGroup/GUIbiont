# Julia files
# Manifest.toml is tracked on purpose: GUIbiont is a deployed application,
# not a library, so the exact dependency resolution is part of what makes
# the published results reproducible.
*.jl.cov
*.jl.*.cov
*.jl.mem

# Data directories (user-specific)
Clean_data/
raw_data/

# Calibration curves are tracked in the repository

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*~
